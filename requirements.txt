cicaid-devtools[dotenv] @ https://github.com/leonarduk/cicaid/releases/download/v0.9.0/cicaid_devtools-0.9.0-py3-none-any.whl
python-dotenv>=1.0  # config.load_config() loads .env; declared directly, not just via cicaid's [dotenv] extra

# Direct imports in version_checker.py (the update check): requests and
# packaging come transitively via cicaid-devtools but are imported directly,
# so they are declared directly (same rule as python-dotenv above).
requests>=2.34.2,<3
packaging>=24,<27
